const keywordsForNextStep = ['next', 'move on', 'move forward', 'continue', 'proceed', 'advance', 'go to the next step'];

const keywordsForSkip = ['skip', 'bypass', 'ignore', 'omit', 'pass', 'leave out', 'skip to next', 'jump ahead'];

module.exports = {
  keywordsForNextStep,
  keywordsForSkip
};